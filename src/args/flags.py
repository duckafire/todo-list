# FLAG: (extended, short)
#
# if `short` is not specified,
# `short == extended[0]`

HELP    = ("help",)
LICENSE = ("license",)
REPLACE = ("replace",)
VERBOSE = ("verbose", "V",)
VERSION = ("version",)
